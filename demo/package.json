{
  "name": "demo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vite": "^5.0.10"
  },
  "dependencies": {
    "broadcast-channel": "^7.0.0",
    "broadcast-channel-storage": "file:.."
  }
}
